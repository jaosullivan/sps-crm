# Local Vite: point at the API (default if unset is http://localhost:8000).
# Production / Docker same-origin: set empty so the browser uses relative /api/...
#   VITE_API_BASE_URL=
VITE_API_BASE_URL=http://localhost:8000
